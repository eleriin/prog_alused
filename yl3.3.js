const readline = require('node:readline');
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});
rl.question("Taringute arv:",T2ringud=>{
    for(let i=0; i<T2ringud;i++)
console.log(Math.ceil(Math.random()*6))

    rl.close()
}
)
